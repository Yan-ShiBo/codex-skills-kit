# 指令审计：2026-09-16

本次依据 OpenAI 的 [Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)，收紧技能触发条件、按需读取参考资料，并用明确完成条件替代固定执行仪式。文章提供审计方向；下列问题与修改来自本机内容复核。

范围为用户管理的 Skills、全局 `AGENTS.md` 及本仓库治理规则。Codex 管理的系统技能、插件包、登录会话和凭据不修改。旧版 `AUDIT.md` 保留为历史记录。

| 类别 | 修改前 | 修改后 |
| --- | --- | --- |
| 过度干活 | 普通 URL 强制触发 Agent Reach；超过工具调用次数就建三份计划；推送附带全面检查、覆盖率要求和额外交付。 | 按交付物和后端需要选择技能；确需跨会话连续性才保存计划；只完成授权范围及相关验证，已有有效结果可复用。 |
| 指令冲突 | gstack 入口重复启动流程、声称覆盖宿主模式；规格默认发布 Issue；描述与实际自动修复行为不一致。 | 根入口只负责路由；宿主模式和用户范围优先；本地规格为默认，发布依请求执行；描述保留真实行为边界。 |
| 无意义等待 | 缺少偏好文件就阻塞；部署固定等 60 秒；重复询问已有授权。 | 使用请求设置和合理默认值；只等已知运行操作或必要输入；检查状态、有界退避，终态即停止。 |

独立复核补正五处：补齐 gstack 命令中的 `$HOME/.claude/skills/gstack` 路径形式；将 Brooks Sweep 限于请求审计并修复；微信摘要描述保留 `wx-cli` 后端限制，避免误接导出文本任务；统一 spec 顶部 HARD GATE 与本地交付规则；删除工程计划审查中泛化的 100% 覆盖和 “Boil the ocean” 要求。必要工具说明、项目约束、认证边界和有效检查继续保留。

修改集中在 `overrides/` 与 `manifest/customizations.json`，安装时从锁定上游重放，避免重装丢失个人精简。每个文件绑定修改前后 SHA-256；本机应用在首次写入前预检全部目标，未知内容拒绝覆盖。已是目标内容则跳过，实现幂等。备份放在 `~/.codex/skill-backups/`，位于技能发现目录之外；恢复时使用对应备份，核对路径与哈希后还原。切换上游版本必须重新审阅；`--latest` 遇到内容不匹配也拒绝应用对应补丁，不能用更新哈希绕过检查。

全局偏好保存在 `overrides/AGENTS.md`，仅显式使用 `--include-preferences` 才应用；已有非空且不同的全局文件须合并，不会直接覆盖。仓库 `AGENTS.md` 则记录补丁维护位置、相关检查及不提交凭据的边界。

本次补丁涉及 **79 个文件**（其中 75 个技能入口），修改前共 **3,687,129 字节**，修改后 **1,441,599 字节**，减少约 **60.9%**。全局 AGENTS.md 模板另行保存。这是受影响文件的文本统计，不代表全部技能体积或实际任务耗时收益。

验证于 **2026-09-17** 完成：

- 仓库清单与路由检查通过；17 项单元测试通过，包含未知内容拒绝、首次写入前预检、备份恢复、幂等、Git 换行和 Windows 长路径回归。
- 从 12 个锁定上游恢复全部 63 个安装目标（62 个技能目标及 1 个共享支持目录）；79 个原始文件的哈希均匹配补丁预期。Python 与原生 PowerShell 分别重放后，79/79 个结果哈希一致。
- 本机应用复查待修改数为 0；75 个受影响技能入口的 YAML 元数据有效；Python 编译与两份 PowerShell 脚本语法检查通过。
- 修复 Git 换行转换导致发布后清单哈希不匹配的问题，显式生成 LF 字节，并验证 Git 暂存对象哈希。隔离恢复暴露的 Windows 长路径问题通过扩展路径处理解决，无需修改系统配置。

本次不宣称所有插件或外部账号渠道已验证可用。Linux 持续集成结果由对应 GitHub 提交的 Actions 状态提供。
